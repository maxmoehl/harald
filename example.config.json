{
  "dial_timeout": "10ms",
  "rules": [
    {
      "listen": {
        "network": "tcp",
        "address": ":60001"
      },
      "connect": {
        "network": "tcp",
        "address": "localhost:8080"
      }
    },
    {
      "listen": {
        "network": "tcp",
        "address": ":60002"
      },
      "connect": {
        "network": "tcp",
        "address": "localhost:8081"
      }
    }
  ]
}
